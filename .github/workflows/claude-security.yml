name: Claude Security Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - 'README.*'
      - '.gitignore'

# Cancel superseded runs on the same PR
concurrency:
  group: claude-security-review-${{ github.ref }}
  cancel-in-progress: true

# Principle of least privilege
permissions:
  contents: read
  pull-requests: write

jobs:
  security-review:
    name: Claude security review
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      EXCLUDE_FILE: .github/security/claude-review/exclude-dirs.txt
    steps:
      - name: Checkout PR
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Restore scanner config from base ref
        run: |
          set -euo pipefail
          BASE_SHA="${{ github.event.pull_request.base.sha }}"
          git fetch --depth=1 origin "$BASE_SHA" 2>/dev/null || true
          for f in scan-instructions.md fp-filters.md exclude-dirs.txt; do
            path=".github/security/claude-review/$f"
            if git cat-file -e "$BASE_SHA:$path" 2>/dev/null; then
              git show "$BASE_SHA:$path" > "$path"
              echo "Restored $f from $BASE_SHA"
            else
              echo "::warning::$f not present on base — using PR version (bootstrap PR only)"
            fi
          done

      - name: Load exclude directories
        id: excludes
        run: |
          set -euo pipefail
          if [[ ! -f "$EXCLUDE_FILE" ]]; then
            echo "::error::Exclude file not found: $EXCLUDE_FILE"
            exit 1
          fi
          # Strip comments and blank lines, join with commas
          DIRS=$(grep -v '^[[:space:]]*#' "$EXCLUDE_FILE" \
                 | grep -v '^[[:space:]]*$' \
                 | sed 's/[[:space:]]*$//' \
                 | paste -sd ',' -)
          echo "Loaded exclude dirs: $DIRS"
          echo "dirs=$DIRS" >> "$GITHUB_OUTPUT"

      - name: Run Claude security review
        uses: anthropics/claude-code-security-review@0c6a49f1fa56a1d472575da86a94dbc1edb78eda
        with:
          claude-api-key: ${{ secrets.ANTHROPIC_SECURITY_SCANNER_API_KEY }}
          comment-pr: true
          upload-results: true
          claude-model: claude-opus-4-6
          claudecode-timeout: 40
          run-every-commit: false
          exclude-directories: ${{ steps.excludes.outputs.dirs }}
          custom-security-scan-instructions: .github/security/claude-review/scan-instructions.md
          false-positive-filtering-instructions: .github/security/claude-review/fp-filters.md
